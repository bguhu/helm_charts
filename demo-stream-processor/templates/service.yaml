apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-stream-processor.fullname" . }}
  labels:
    {{- include "demo-stream-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-stream-processor.selectorLabels" . | nindent 4 }}
