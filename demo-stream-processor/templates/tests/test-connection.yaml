apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-stream-processor.fullname" . }}-test-connection"
  labels:
    {{- include "demo-stream-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-stream-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
