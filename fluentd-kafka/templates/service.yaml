apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluentd-kafka.fullname" . }}
  labels:
    {{- include "fluentd-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluentd-kafka.selectorLabels" . | nindent 4 }}
