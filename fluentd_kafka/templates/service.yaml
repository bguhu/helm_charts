apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluentd_kafka.fullname" . }}
  labels:
    {{- include "fluentd_kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluentd_kafka.selectorLabels" . | nindent 4 }}
